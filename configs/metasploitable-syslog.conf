# Legacy host: /etc/syslog.conf
#
# Forward all facilities and priorities to the Wazuh manager over UDP 514.
# Must start at the LEFT MARGIN with a tab between the selector and the target.
# Apply with a syslog daemon restart.
#
# Editing note (docs/issues-log.md I24): nano fails on this host's terminfo
# database — use vi, or set TERM=xterm for the session.

*.*     @192.168.1.13
